IF editing `cli/**` or `skill/Source.md`:
### get entity shape from the schema catalog
Fetch `GET /api/v1/schema/` for entity names, actions, fields, modes, and parameters; do not add hardcoded entity lists or pluralization guesses.
