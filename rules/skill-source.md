IF editing `skill/**`:
### keep authored references separate from hardening code
Change the CLI, build pipeline, and `skill/Source.md` for operator behavior; leave authored prose under `skill/references/` to the agent that owns that writing.
