IF editing `cli/**`:
### verify credentials before saving
Call the tenant schema catalog with the exact Dent Site URL and Dent bearer credential before writing config, so a bad credential or wrong Site fails during login.
